<!-- image properties -->
<properties>

	<!-- image borders -->
	<!-- these determine how many pixels any content is inset from each side of the image -->
	<!-- (for example, in the case of a button, the content would be the text label) -->
	<record id="image_borders" left="10" right="10" top="10" bottom="10"/>

	<!-- image slices determine the points at which the image will be sliced to enable scaling -->
	<!-- the image will only slice in the directions slicing values are provided for-->
	<record id="image_slices" left="10" right="10" top="1" bottom="1"/>

	<record id="font_spec">
		<colour id="colour" name="text"/>
	</record>

	<colour id="red_replacement" name="text"/>
	<colour id="blue_replacement" name="text"/>

	<record id="primary_icon_properties" red_replacement="text" />
	<record id="secondary_icon_properties" red_replacement="text"/>

</properties>
